I share same settings for Cursor and VSCode.
The settings are stored in the `dotfiles/vscode` directory.
These settings are applied to both Cursor and VSCode by symlinking the files to the appropriate location done by `setup.sh`.
